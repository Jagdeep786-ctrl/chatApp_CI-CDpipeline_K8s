apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: nginx
spec:
  selector:
    app: nginx
  ports:
  - port: 80 
    protocol: TCP
    targetPort: 80
  type: ClusterIP


    # kubectl apply -f service.yaml
    # kubectl get svc -n nginx
    #  kubectl describe svc nameofsvc -n nginx
    